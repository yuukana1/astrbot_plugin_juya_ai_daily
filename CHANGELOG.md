# Changelog

## [1.0] - 2026-08-06

### 变更
- 对外指令改为中文：`/AI日报`、`/AI日报订阅`、`/AI日报退订`、`/AI日报状态`。
- 移除 LLM 总结调用、LLM 供应商配置和相关指令。
- 移除图片失败后的 AI 摘要和完整 RSS 纯文本兜底。
- 图片渲染重试耗尽后发送“图片渲染失败”提示；图片发送重试耗尽后发送“图片发送失败”提示。
- 失败提示发送成功后目标视为本轮已处理；失败提示也无法发送时保留目标待重试。

## [1.2.5] - 2026-08-06

### 优化
- 公开版本未提供可选字体时记录普通信息日志，不再把正常的系统字体回退标记为警告。

## [1.2.4] - 2026-08-06

### 公开发布清理
- 移除未确认授权的汉仪润圆字体二进制文件；字体仅作为部署者自行提供的可选资源。
- 移除旧的占位 GitHub 仓库地址，待发布者填写自己的仓库地址。
- 修正 README 的许可证名称为 AGPL-3.0-or-later，并加入公开发布说明。

## [1.2.3] - 2026-08-06

### 修复
- 定时推送与手动 `/ainews` 使用独立去重域；当天定时图片刚发送成功也不会拦截新的手动获取。
- 手动去重改为按平台消息 ID 识别同一条命令事件，不会误伤用户主动发送的新命令。
- 继续保持每条手动命令最多一次图片发送，重复事件和并发处理不会连发两张。

## [1.2.2] - 2026-08-06

### 修复
- 将字体文件移到插件根目录，避免部分 AstrBot 文件上传安装环境未保留 `assets` 子目录时回退系统字体。
- 字体加载器同时兼容根目录和旧版 `assets` 路径，并校验 WOFF2 文件头。

## [1.2.1] - 2026-08-06

### 修复
- 手动 `/ainews` 不再写入定时推送完成状态，避免提前执行命令后订阅群漏收当日自动推送。
- 手动图片采用单次发送语义，并以“文章 + 会话”加锁和短时去重，避免重复事件、发送重试或 URL/本地切换造成两张图片。
- 内存图片缓存可正确识别 AstrBot 渲染服务返回的 URL，不再把 URL 误当作本地路径。
- 发布 ZIP 改为 AstrBot v4.27.1 兼容的根目录平铺结构。

### 新增
- 使用用户指定的“汉仪润圆 55S”，预转换为 WOFF2 后按需嵌入 HTML 模板。
- 新增 `manual_dedupe_seconds` 配置，默认 60 秒。

### 优化
- 字体转换仅在构建阶段完成，服务器不需要 FontTools、Brotli 或系统字体安装；Base64 数据只在实际渲染期间存在。

## [1.2.0] - 2026-08-06

### 新增
- 图片渲染、URL 图片发送、本地文件发送均支持指数退避重试。
- 图片链路重试耗尽后自动调用 AstrBot 官方 `Context.llm_generate` 生成文字摘要；模型不可用时回退为全量 RSS 文字版。
- 新增渲染熔断、可配置时区、发送并发和本地图片缓存清理。
- `/ainews_status` 增加熔断状态、待重试目标、最近 RSS 检查与最近投递结果。

### 修复
- 逐目标持久化投递结果，避免“一个群成功就把整期标记成功”导致其他群永久漏发。
- 检查 `Context.send_message` 的布尔结果；下一轮只重试失败目标。
- 已推送链接按稳定顺序保留，不再由无序集合随机裁剪。

### 优化
- RSS 请求复用一个 `aiohttp.ClientSession`，并支持 ETag / Last-Modified 条件请求。
- 文件 JSON 与图片均原子写入，阻塞式磁盘操作移至工作线程。
- AI 总结按文章内容哈希缓存，并用互斥锁避免多目标并发重复调用模型。

## [1.1.0] - 2026-08-06

### 新增
- 使用 AstrBot 官方 `html_render` 与 Jinja2 模板生成 AI 日报图片。
- 从 RSS 原始 HTML 提取标题、导语、栏目和来源，手动指令与定时推送均发送图片。
- 新增图片开关和 JPEG 质量配置。

### 优化
- 同一期图片仅渲染一次，全部 RSS 更新按栏目顺序合并为一张高清长图。
- 渲染超时、RSS 结构变化或服务异常时自动回退文字消息。

## [1.02] - 2026-06-14

### 变更
- **RSS 源迁移**：更新橘鸦 AI 日报 RSS 订阅地址为 `https://daily.juya.uk/rss.xml`，替换已废弃的旧地址。
- **完整正文解析**：适配新 RSS 源的 `content:encoded` 字段，优先读取完整正文，缺失时回退到 `description` 摘要。
- **日期识别优化**：优先使用标题中的 `YYYY-MM-DD` 日期，避免发布时间时区差影响当日推送判断。

## [1.01] - 2026-03-12

### 新增
- **多平台支持**：订阅目标支持群聊（`GroupMessage`）与私聊（`FriendMessage`）两种类型，可在配置中分别填写群聊 ID 与私聊账号 ID，格式兼容 `{机器人名称}:{ID}`、纯数字等多种写法。
- **AI 总结开关**：新增 `enable_ai_summary` 配置项，可关闭 AI 总结功能，关闭后直接推送原文摘要，不再调用 LLM。
