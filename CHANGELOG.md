# Changelog

## [1.0.0] - 2026-08-06

### 变更

- 插件内部标识统一为 `astrbot_plugin_juya_ai_daily`。
- 版本号统一为符合语义化版本规范的 `1.0.0`。
- 对外指令统一为 `/AI日报`、`/AI日报订阅`、`/AI日报退订`、`/AI日报状态`。
- `/AI日报订阅` 同时支持群聊和私聊，订阅目标为发送指令的当前会话。
- 将 `metadata.yaml` 的版本号显式声明为字符串，兼容 AstrBot v4.27.1 上传校验。
- 状态指令按群聊和私聊分类显示具体会话 ID、机器人名称和订阅来源。
- 手动获取不再发送“正在从 RSS 获取”提示，只返回图片或对应失败提示。
- 默认安装包内置霞鹜文楷轻便版 Regular v1.522，另提供不含字体的轻量安装包。
- 字体仅在实际渲染时读取并临时编码，缺失或损坏时自动回退系统字体。
- 精简状态输出，移除无实际操作价值的日报字体条目。
- 按 AstrBot 官方规范新增根目录 `logo.png` 插件头像（256×256）。
- 移除日报底部固定句子，并重新收紧页脚分隔线与信息区间距。
- 日报画布自适应渲染视口宽度，消除渲染服务视口变化导致的概率性右侧白边。
- 获取 RSS 当期全部更新，并按原栏目顺序合并为一张高清长图。
- 手动获取每条命令最多尝试发送一张图片，避免重复投递。
- 图片渲染或发送重试耗尽后，仅发送对应的失败提示。
- 不调用模型，也不发送完整 RSS 纯文本兜底。
- 清理未使用的代码、说明和历史发布记录。
